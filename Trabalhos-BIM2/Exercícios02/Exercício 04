#include <iostream>
using namespace std;

int soma(int a, int b) {
    return a + b;
}

int sub(int a, int b) {
    return a - b;
}

int mult(int a, int b) {
    return a * b;
}

double divi(int a, int b) {
    return (double)a / b;
}

int main() {
    int a, b;

    cin >> a >> b;

    cout << "Soma: " << soma(a, b) << endl;
    cout << "Subtracao: " << sub(a, b) << endl;
    cout << "Multiplicacao: " << mult(a, b) << endl;
    cout << "Divisao: " << divi(a, b) << endl;

    return 0;
}
